export const config = {
    loungeLimit =  50,
    sharePercantage = 15,
    statusTimer = 15,
    statusTimerSlowTimeLimit = 30,
    statusTimerRateLimitInterval = 5,
    textDelay = 3,
    roomDelay = 8,
    deviationAmountForDistrubution = 5
}

const autoConfig = {
    //we will use an analysis tool to change paramaters for the users (this is too future)
}
